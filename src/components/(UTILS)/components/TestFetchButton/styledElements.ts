import { newStyledElement } from "@setsu-tp/styled-components";
import styles from "./styles.module.css";

export const TestFetchButtonContainer = newStyledElement.div(
	styles.testFetchButtonContainer
);
