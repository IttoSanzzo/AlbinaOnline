import { newStyledElement } from "@setsu-tp/styled-components";
import styles from "./styles.module.css";

export const SortableLinkContainer = newStyledElement.div(
	styles.sortableLinkContainer
);
export const DragButton = newStyledElement.button(styles.dragButton);
