import { newStyledElement } from "@setsu-tp/styled-components";
import styles from "./styles.module.css";

export const DialogContainer = newStyledElement.div(styles.dialogContainer);
export const DialogButtonsContainer = newStyledElement.div(
	styles.dialogButtonsContainer
);
