import { newStyledComponent } from "@setsu-tp/styled-components";
import styles from "./styles.module.css";

export const FavoriteButtonContainer = newStyledComponent.div(styles.favoriteButtonContainer);
